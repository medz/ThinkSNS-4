// metadata has authorization
export const AUTH_METADATA_HAS_AUTHORIZATION = 'metadata has authorization';

// metadata has authorization type
export const AUTH_METADATA_HAS_AUTHORIZATION_TYPE =
  'metadata has authorization type';

// auth token validity period key
export const AUTH_TOKEN_VVALIDITY_PERIOD_KEY = 'auth token validity period key';

// token default expired in.
export const AUTH_TOKEN_DEFAULT_EXPIRED_IN = 1;
// token default expired in unit type.
export const AUTH_TOKEN_DEFAULT_EXPIRED_UNIT = 'days';

// token default refresh expired in.
export const AUTH_TOKEN_DEFAULT_REFRESH_EXPIRED_IN = 1;
// token default refresh expired in unit type.
export const AUTH_TOKEN_DEFAULT_REFRESH_EXPIRED_UNIT = 'months';
